name: "CodeQL config"

queries:
  - uses: security-extended
  - uses: security-and-quality

paths-ignore:
  # Exclude third-party dependencies generated by CMake (FetchContent/ExternalProject)
  - "build/_deps/**"
  - "**/build/_deps/**"
